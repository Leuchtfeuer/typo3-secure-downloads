# cat=basic; type=string; label=securedDirs:a list of dirs that should be secured separated by a pipe
securedDirs = typo3temp|fileadmin|uploads

# cat=basic; type=string; label=filetype:a list of filetypes that should be secured separated by a pipe
filetype = pdf|jpe?g|gif|png|odt|ppt?x|doc?x|xls?x|zip|rar|tgz|tar|gz

# cat=basic; type=boolean; label = forcedownload: Force Download of specified filetypes
forcedownload = 0

# cat=basic; type=string; label=forcedownloadtype:a list of filetypes that should not be opened inline in a browser, seperated by a pipe. Only used if forcedownload is true
forcedownloadtype = odt|ppt?x|doc?x|xls?x|zip|rar|tgz|tar|gz

# cat=basic; type=string; label=additionalMimeTypes: Comma separated list of additional MIME types (file extension / mime type pairs, in which file extension and MIME type is separated by a pipe symbol). Can be used to override existing MIME type settings of the extension as well.
additionalMimeTypes = txt|text/plain,html|text/html

# cat=basic; type=string; label=domain:a domain prefix before the securedDirs part (e.g. http://www.host.com/). Not needed for internal (relative) links.
domain = http://mydomain.com|http://my.other.domain.org

# cat=basic; type=int; label=cachetimeadd:additional time added to the cache time of the page or to the default cache time (default 3600sec = 1h)
cachetimeadd = 3600

# cat=basic; type=int[0-3]; label = debug level (output each tag that maches the regex and will be replaced)
debug = 0

# cat=basic; type=boolean; label = log:Log each file access
log = 0

# cat=basic; type=string; label = outputFunction:PHP function which is used to deliver the secured files (one of readfile, readfile_chunked, fpassthru)
outputFunction = readfile

# cat=basic; type=int; label = outputChunkSize:Size of chunks to be deliverd in one go (only relevant if readfile_chunked is used) (default: 1048576 Bytes)
outputChunkSize = 1048576

# cat=basic; type=string; label=linkFormat:The Format for the link generated by naw_securedl. You can change this if you apply appropriate mod_rewrite rules in your .htaccess file. Example: linkFormat: 'securedl/###FEUSER###/###FEGROUPS###/###TIMEOUT###/###HASH###/###FILE###' Put the following in your .htaccess 'RewriteRule ^securedl/([\d]+)/([\d]+)/([\d]+(,[\d])*)/([0123456789abcdef]+)/([^&]+)$ index.php?eID=tx_nawsecuredl&u=$1&t=$2&hash=$3&file=$4 [nc,L]'.
linkFormat = index.php?eID=tx_nawsecuredl&u=###FEUSER###&g=###FEGROUPS###&t=###TIMEOUT###&hash=###HASH###&file=###FILE###

# cat=basic; type=boolean; label = enableGroupCheck: Allows file download if the user has at least one group in common with the list of transmitted groups. Enabling this, makes the checks LESS restrictive!
enableGroupCheck = 0

# cat=basic; type=string; label=groupCheckDirs:A list of directories that should enable the less restrictive group check, separated by a pipe. Leave empty if you want to enable the group check for all directories.
groupCheckDirs =

# cat=basic; type=string; label=excludeGroups:A comma separated list of groups that are excluded from the group check if enabled
excludeGroups =

